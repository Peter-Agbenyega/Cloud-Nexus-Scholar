import { ModuleStub } from "@/components/module-stub";

export default function SandboxPage() {
  return (
    <ModuleStub
      eyebrow="Sandbox"
      title="Secure practice and experimentation module"
      description="The sandbox will later become the learner’s private space for guided applied exercises tied to the UMGC roadmap. For now it is a route-safe placeholder that keeps the product structure coherent without inventing functionality ahead of schedule."
      nextSlice="Applied scenarios and future cyber-cloud sandbox behavior"
    />
  );
}
