import { ModuleStub } from "@/components/module-stub";

export default function PlannerPage() {
  return (
    <ModuleStub
      eyebrow="Planner"
      title="Personal academic planning module"
      description="This will become the learner’s private scheduling and sequencing surface for the UMGC path. In this slice it exists only as a structural destination so the app already reads like a personal academic operating system."
      nextSlice="Planner logic, sequencing rules, and workload orchestration"
    />
  );
}
