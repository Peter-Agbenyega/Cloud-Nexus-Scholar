import { ModuleStub } from "@/components/module-stub";

export default function LibraryPage() {
  return (
    <ModuleStub
      eyebrow="Library"
      title="Private concept and reference library"
      description="This module will eventually hold concept memory, explanations, source material, and cross-course knowledge links for the single learner. In this slice it exists as a clean workspace stub so the navigation already reflects the long-term product shape."
      nextSlice="Concept library expansion, source ingestion, and future search hooks"
    />
  );
}
